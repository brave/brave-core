import("//build/config/features.gni")

source_set("renderer") {
  visibility = [
    "//chrome/renderer/*",
    "//brave/renderer/*",
    "//brave:child_dependencies",
    "//brave/test:*"
  ]

  public_deps = [
   "//chrome/renderer",
  ]

  if (is_android) {
    deps = [
      "//chrome/common",
      "//brave/components/cosmetic_filters/content/renderer",
    ]
  }
}
