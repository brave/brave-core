name: Trigger sync PR from fork

on:
  pull_request_review:
    types: [submitted]

permissions:
  actions: write # Required to trigger other workflows

jobs:
  verify-membership:
    if: ${{ !github.event.review.user.bot && startsWith(github.event.review.body, '/run-ci') }}
    runs-on: ubuntu-slim
    outputs:
      is_member: ${{ steps.check.outputs.is_member }}
    steps:
      - name: Query GitHub API for Membership
        id: check
        uses: actions/github-script@v7
        with:
          script: |
            const commenter = context.payload.review.user.login;
            const orgName = context.repo.owner;

            try {
              const membership = await github.rest.orgs.checkMembershipForUser({
                org: orgName,
                username: commenter,
              });

              if (membership.status === 204) {
                core.setOutput('is_member', 'true');
                return;
              }
            } catch (error) {
              console.log(`User ${commenter} is not in org ${orgName}.`);
            }

            core.setOutput('is_member', 'false');

  trigger-workflow:
    needs: verify-membership
    if: ${{ needs.verify-membership.outputs.is_member == 'true' }}
    runs-on: ubuntu-slim
    steps:
      - name: Dispatch Target with Strict Snapshot Data
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.payload.pull_request.number;
            const commitHash = context.payload.review.commit_id;

            // Strict Validation: If GitHub payload has no locked commit hash, fail immediately
            if (!commitHash) {
              core.setFailed("CRITICAL ERROR: Unable to securely resolve the immutable review.commit_id snapshot. Terminating process to prevent race conditions.");
              return;
            }

            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'sync-pr-from-fork.yml',
              ref: 'main',
              inputs: {
                PR_NUMBER: String(prNumber),
                COMMIT_HASH: commitHash
              }
            });
