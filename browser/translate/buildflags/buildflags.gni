import("//brave/build/config.gni")
import("//extensions/buildflags/buildflags.gni")

declare_args() {
  enable_brave_translate_go = true
}

declare_args() {
  enable_brave_translate_extension =
      !enable_brave_translate_go && enable_extensions
}
