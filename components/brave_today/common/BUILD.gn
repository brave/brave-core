import("//brave/build/config.gni")

source_set("common") {
  sources = [
    "pref_names.cc",
    "pref_names.h",
    "switches.cc",
    "switches.h",
    "urls.cc",
    "urls.h",
  ]

  deps = [ "//base" ]
}
