import("//brave/components/payments/content/buildflags/buildflags.gni")
import("//build/buildflag_header.gni")

buildflag_header("buildflags") {
  header = "buildflags.h"
  flags = [ "ENABLE_PAY_WITH_BAT=$enable_pay_with_bat" ]
}
