import("//brave/components/rpill/buildflags.gni")
import("//build/buildflag_header.gni")

buildflag_header("buildflags") {
  header = "buildflags.h"
  flags = [ "RPILL_ENABLED=$rpill_enabled" ]
}

source_set("rpill") {
  check_includes = false
  public_deps = []

  sources = [
    "browser/rpill.cc",
    "browser/rpill.h",
  ]

  deps = [ "//base" ]
}
