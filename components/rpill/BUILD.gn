import("//brave/components/rpill/buildflags.gni")
import("//build/buildflag_header.gni")

buildflag_header("buildflags") {
  header = "buildflags.h"
  flags = [ "ENABLE_RPILL=$enable_rpill" ]
}

source_set("rpill") {
  public_deps = [ ":buildflags" ]

  sources = [
    "rpill.cc",
    "rpill.h",
  ]

  deps = [ "//base" ]
}
