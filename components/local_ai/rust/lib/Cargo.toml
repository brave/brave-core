[package]
name = "candle-embedder"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"

[dependencies]
candle-core = "0.9.1"
candle-nn = "0.9.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokenizers = { version = "0.21.0", default-features = false, features = ["fancy-regex"] }
