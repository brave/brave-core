################################################################################
# LeakSanitizer Suppressions file.
# Brave-specific addition to //build/sanitizers/lsan_suppressions.cc
# Please annotate each suppression with a related issue.
################################################################################

# https://github.com/brave/brave-browser/issues/52522
leak:update_client::UpdateEngine
leak:update_client::TaskUpdate

# https://github.com/brave/brave-browser/issues/52743
leak:skus::initialize_sdk

### Permanent suppressions
# Clipboard data can "leak" if the browser is closing the clipboard data is
# processed. Also happens in chromium test browsers.
leak:ui::X11ClipboardOzone::RequestClipboardData
