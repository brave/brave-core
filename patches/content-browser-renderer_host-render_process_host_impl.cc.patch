diff --git a/content/browser/renderer_host/render_process_host_impl.cc b/content/browser/renderer_host/render_process_host_impl.cc
index 2e5e7c71a529f3d9708c7f6c53cc89f03bdb842e..7cebbc5d186b7cf48e2ab1822ec9035c0cb4120c 100644
--- a/content/browser/renderer_host/render_process_host_impl.cc
+++ b/content/browser/renderer_host/render_process_host_impl.cc
@@ -3379,12 +3379,19 @@ void RenderProcessHostImpl::AppendRendererCommandLine(
     command_line->AppendSwitch(switches::kNoZygote);
   }
 
+  std::string js_flags;
   if (IsJitDisabled()) {
-    command_line->AppendSwitchASCII(blink::switches::kJavaScriptFlags,
-                                    "--jitless");
+    js_flags = "--jitless";
   } else if (AreV8OptimizationsDisabled()) {
+    js_flags = "--disable-optimizing-compilers";
+  }
+  if (!js_flags.empty()) {
+    if (command_line->HasSwitch(blink::switches::kJavaScriptFlags)) {
+      base::StrAppend(&js_flags, {",", command_line->GetSwitchValueASCII(
+                                            blink::switches::kJavaScriptFlags)});
+    }
     command_line->AppendSwitchASCII(blink::switches::kJavaScriptFlags,
-                                    "--disable-optimizing-compilers");
+                                    js_flags);
   }
 
   if (DisallowV8FeatureFlagOverrides()) {
