diff --git a/cc/BUILD.gn b/cc/BUILD.gn
index eaa675ba6a9ff49f50d972465d37b20f0be3462f..b281677b19836fbc886780bff8944d03a8904ac1 100644
--- a/cc/BUILD.gn
+++ b/cc/BUILD.gn
@@ -11,7 +11,24 @@ if (is_android) {
   import("//build/config/android/rules.gni")
 }
 
+config("cc_cflags") {
+  cflags = []
+
+  if (is_clang) {
+    cflags += [ "-Wno-undefined-bool-conversion" ]
+  }
+
+  if (is_win) {
+    # TODO(vmpstr): Some SoftwareImageDecodeCacheTests use virtual inheritance,
+    # which MSVC doesn't like. Suppress "Foo inherits Bar via dominance"
+    # warnings for now.
+    cflags += [ "/wd4250" ]
+  }
+}
+
 cc_component("cc") {
+  configs = [ ":cc_cflags" ]
+
   sources = [
     "benchmarks/benchmark_instrumentation.cc",
     "benchmarks/benchmark_instrumentation.h",
