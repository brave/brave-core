diff --git a/chrome/browser/ui/bookmarks/bookmark_bar_controller.cc b/chrome/browser/ui/bookmarks/bookmark_bar_controller.cc
index 77fec75f06f7ad02840ce764da2ef984cb0f6346..e8a8a596104bdcb0c0e6b68e8e9644f32ec9e251 100644
--- a/chrome/browser/ui/bookmarks/bookmark_bar_controller.cc
+++ b/chrome/browser/ui/bookmarks/bookmark_bar_controller.cc
@@ -173,7 +173,7 @@ bool BookmarkBarController::ShouldShowBookmarkBar() const {
 
   // The bookmark bar is only shown if the user has added something to it.
   if (!has_bookmarks && !has_saved_tab_groups) {
-    return false;
+    // return false;
   }
 
   // Prevent the bookmark bar from showing itself when entering fullscreen if
@@ -187,7 +187,7 @@ bool BookmarkBarController::ShouldShowBookmarkBar() const {
   return std::any_of(
       tabs.begin(), tabs.end(), [](const tabs::TabInterface* tab) {
         return tab->GetContents() && IsShowingNTP(tab->GetContents());
-      });
+      }) && (prefs->GetBoolean(kAlwaysShowBookmarkBarOnNTP) || prefs->GetBoolean(bookmarks::prefs::kShowBookmarkBar));
 }
 
 void BookmarkBarController::OnTabStripModelChanged(
