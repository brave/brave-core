diff --git a/chrome/browser/ui/search_engines/search_engine_tab_helper.cc b/chrome/browser/ui/search_engines/search_engine_tab_helper.cc
index 3dbb17ed3a604263c39905fa4d97e97b68745f42..6cceba9e1ab81317e3c0e3f09f957a5632ffbac0 100644
--- a/chrome/browser/ui/search_engines/search_engine_tab_helper.cc
+++ b/chrome/browser/ui/search_engines/search_engine_tab_helper.cc
@@ -111,7 +111,8 @@ void SearchEngineTabHelper::PageHasOpenSearchDescriptionDocument(
     return;
 
   Profile* profile =
-      Profile::FromBrowserContext(web_contents()->GetBrowserContext());
+      Profile::FromBrowserContext(web_contents()->GetBrowserContext()); 
+  PREVENT_OSDD_ADDITION
   if (page_url != web_contents()->GetLastCommittedURL() ||
       !TemplateURLFetcherFactory::GetForProfile(profile) ||
       profile->IsOffTheRecord())
@@ -169,6 +170,7 @@ void SearchEngineTabHelper::GenerateKeywordIfNecessary(
 
   Profile* profile =
       Profile::FromBrowserContext(web_contents()->GetBrowserContext());
+  PREVENT_OSDD_ADDITION
   if (profile->IsOffTheRecord())
     return;
 
