diff --git a/chrome/browser/browsing_topics/browsing_topics_internals_browsertest.cc b/chrome/browser/browsing_topics/browsing_topics_internals_browsertest.cc
index 49504cadb6663..b7c3a54bf1303 100644
--- a/chrome/browser/browsing_topics/browsing_topics_internals_browsertest.cc
+++ b/chrome/browser/browsing_topics/browsing_topics_internals_browsertest.cc
@@ -680,6 +680,11 @@ IN_PROC_BROWSER_TEST_F(BrowsingTopicsInternalsBrowserTest,
   EXPECT_TRUE(ui_test_utils::NavigateToURL(browser(),
                                            GURL(kBrowsingTopicsInternalsUrl)));
 
+  // FlushForTesting() ensures the GetModelInfo mojo call and the 0-delay task
+  // in TestAnnotator::NotifyWhenModelAvailable have completed before checking
+  // the DOM. Without this, the async GetModelInfo round-trip may not finish
+  // before GetModelInfoContent() evaluates the JavaScript.
+  FlushForTesting();
   EXPECT_EQ(GetModelInfoContent(),
             R"(Model unavailable.
 )");
@@ -706,6 +711,11 @@ IN_PROC_BROWSER_TEST_F(BrowsingTopicsInternalsBrowserTest, ClassifierTab) {
   EXPECT_TRUE(ui_test_utils::NavigateToURL(browser(),
                                            GURL(kBrowsingTopicsInternalsUrl)));
 
+  // FlushForTesting() ensures the GetModelInfo mojo call and the 0-delay task
+  // in TestAnnotator::NotifyWhenModelAvailable have completed before checking
+  // the DOM. Without this, the async GetModelInfo round-trip may not finish
+  // before GetModelInfoContent() evaluates the JavaScript.
+  FlushForTesting();
   EXPECT_EQ(GetModelInfoContent(),
             R"(Model version: 1
 Model file path: /test_path/test_model.tflite
