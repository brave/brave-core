diff --git a/media/base/media_switches.cc b/media/base/media_switches.cc
index aa96f61b4419ec454d26c86f277d33d38347c84b..e6735b485313c36213a17ebdf5aa198b524cdbc3 100644
--- a/media/base/media_switches.cc
+++ b/media/base/media_switches.cc
@@ -554,7 +554,8 @@ BASE_FEATURE(kEnableHighChannelLayouts, base::FEATURE_ENABLED_BY_DEFAULT);
 BASE_FEATURE(kEnableRtcpReporting, base::FEATURE_ENABLED_BY_DEFAULT);
 
 // Enables user control over muting tab audio from the tab strip.
-BASE_FEATURE(kEnableTabMuting, base::FEATURE_DISABLED_BY_DEFAULT);
+// kEnableTabMuting feature state is enforced via plaster rewrite.
+BASE_FEATURE(kEnableTabMuting, base::FEATURE_ENABLED_BY_DEFAULT);
 
 // Enables tracking the occlusion of HW secure encrypted video elements.
 // The HW secure check is OS agnostic, but this switch is currently only
@@ -711,7 +712,8 @@ BASE_FEATURE(kInternalMediaSession,
 );
 
 // Enables the Live Caption feature on supported devices.
-BASE_FEATURE(kLiveCaption, base::FEATURE_ENABLED_BY_DEFAULT);
+// kLiveCaption feature state is enforced via plaster rewrite.
+BASE_FEATURE(kLiveCaption, base::FEATURE_DISABLED_BY_DEFAULT);
 
 // Enable automatic downloading of speech recognition language packs.
 BASE_FEATURE(kLiveCaptionAutomaticLanguageDownload,
