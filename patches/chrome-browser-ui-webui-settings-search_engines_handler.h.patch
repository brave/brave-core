diff --git a/chrome/browser/ui/webui/settings/search_engines_handler.h b/chrome/browser/ui/webui/settings/search_engines_handler.h
index 5e6c5e3c9d892cff9c29c18ee7cd7e5ab36c3a68..79398fae5b58c3d143f1ae415910f76dae575ac2 100644
--- a/chrome/browser/ui/webui/settings/search_engines_handler.h
+++ b/chrome/browser/ui/webui/settings/search_engines_handler.h
@@ -52,6 +52,7 @@ class SearchEnginesHandler : public SettingsPageUIHandler,
   void OnJavascriptDisallowed() override;
 
  private:
+  friend class BraveSearchEnginesHandler;
   friend class SearchEnginesHandlerTest;
 
   // Retrieves all search engines and returns them to WebUI.
@@ -63,7 +64,7 @@ class SearchEnginesHandler : public SettingsPageUIHandler,
   // TODO (crbug.com/494551138): Remove once `SearchSettingsUpdate` is launched.
   void HandleGetSearchEnginesList(const base::ListValue& args);
 
-  base::DictValue GetSearchEnginesList();
+  virtual base::DictValue GetSearchEnginesList();
 
   // Returns whether the search engine choice should be saved in guest mode
   // Returns null if the profile is not eligible for guest choice saving.
