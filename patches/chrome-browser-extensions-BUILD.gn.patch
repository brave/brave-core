diff --git a/chrome/browser/extensions/BUILD.gn b/chrome/browser/extensions/BUILD.gn
index 358b0a3b16fa..00b8a96920af 100644
--- a/chrome/browser/extensions/BUILD.gn
+++ b/chrome/browser/extensions/BUILD.gn
@@ -930,6 +930,7 @@ static_library("extensions") {
     "//ui/strings",
     "//url",
   ]
+  if (is_android) {deps -= ["//chrome/browser/media/router/discovery",]}
 
   if (enable_autofill_assistant_api) {
     sources += [
@@ -1117,12 +1118,12 @@ static_library("extensions") {
     }
   }
 
-  if (!is_android) {
+  #if (!is_android) {
     sources += [
       "api/processes/processes_api.cc",
       "api/processes/processes_api.h",
     ]
-  }
+  #}
 
   if (is_win || is_mac) {
     sources += [
@@ -1171,7 +1172,7 @@ static_library("extensions") {
     ]
   }
 
-  if (enable_plugins) {
+  if (enable_plugins && !is_android) {
     sources += [
       "plugin_manager.cc",
       "plugin_manager.h",
