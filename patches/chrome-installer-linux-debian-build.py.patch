diff --git a/chrome/installer/linux/debian/build.py b/chrome/installer/linux/debian/build.py
index 0f5000cb022f04cafdcec3cf119cf0aa2f85ea5a..cac187951c9bc1b59dec9648a028ca47d79bac27 100755
--- a/chrome/installer/linux/debian/build.py
+++ b/chrome/installer/linux/debian/build.py
@@ -198,6 +198,9 @@ def main():
 
     os.environ["DEB_HOST_ARCH"] = args.arch
     pkg_name = f"{inst.context['PACKAGE_ORIG']}-{channel}"
+    if channel == "stable":
+        pkg_name = f"{inst.context['PACKAGE_ORIG']}"
+    zip_path = output_dir / f"{pkg_name}-{inst.context['VERSIONFULL']}-linux-{args.arch}.zip"; subprocess.check_call(["zip", "-qr", str(zip_path), "."], cwd=str(install_dir))
 
     if deb_control.exists():
         gen_control(
