diff --git a/chrome/installer/linux/debian/build.py b/chrome/installer/linux/debian/build.py
index 4c8c99046294dbcdb7bac607af383202faf1b22e..80f0f280c5de7dcc15eee5a8b52900732f85e98d 100755
--- a/chrome/installer/linux/debian/build.py
+++ b/chrome/installer/linux/debian/build.py
@@ -172,6 +172,8 @@ def main() -> None:
 
         os.environ["DEB_HOST_ARCH"] = args.arch
         pkg_name = f"{config.package_orig}-{config.channel}"
+        if config.channel == "stable": pkg_name = f"{inst.context['PACKAGE_ORIG']}"
+        zip_path = (output_dir / f"{pkg_name}-{inst.context['VERSIONFULL']}-linux-{args.arch}.zip").resolve(); subprocess.check_call(["zip", "-qr", str(zip_path), "."], cwd=str(install_dir))
 
         if deb_control.exists():
             gen_control(
