diff --git a/third_party/blink/renderer/core/html/parser/BUILD.gn b/third_party/blink/renderer/core/html/parser/BUILD.gn
index 16e082d52f6fcdfea1e36b49e2827b23293fec75..4785545bd8af5abf8dc729c2b82d1fee459d2eb8 100644
--- a/third_party/blink/renderer/core/html/parser/BUILD.gn
+++ b/third_party/blink/renderer/core/html/parser/BUILD.gn
@@ -4,6 +4,7 @@
 
 import("//testing/libfuzzer/fuzzer_test.gni")
 import("//third_party/blink/renderer/core/core.gni")
+import("//brave/vendor/speedreader_rust_ffi/config.gni")
 
 blink_core_sources("parser") {
   sources = [
@@ -79,6 +80,14 @@ blink_core_sources("parser") {
     "xss_auditor_delegate.h",
   ]
 
+  public_deps = [
+    ":speedreader_libs",
+  ]
+
+  deps = [
+      "//brave/vendor/speedreader_rust_ffi:speedreadder_ffi",
+  ]
+
   # Optimizing the HTML parser for speed yields significant gains in performance
   # in parser-heavy scenarios. See https://crbug.com/787512.
   # Windows builds already override the default optimization in core.gni.
@@ -97,3 +106,21 @@ fuzzer_test("blink_html_tokenizer_fuzzer") {
     "../../../platform:blink_fuzzer_test_support",
   ]
 }
+
+if (is_mac) {
+  bundle_data("speedreader_libs") {
+    print(speedreader_lib_path)
+    sources = [
+      speedreader_lib_path,
+    ]
+    outputs = [
+      "{{bundle_contents_dir}}/Libraries/{{source_file_part}}",
+    ]
+    public_deps = [
+      "//brave/vendor/speedreader_rust_ffi:speedreader",
+    ]
+  }
+} else {
+  group("speedreader_libs") {}
+}
+
