diff --git a/chrome/browser/ui/browser.cc b/chrome/browser/ui/browser.cc
index 0f1344d9c357..cca72b40b83a 100644
--- a/chrome/browser/ui/browser.cc
+++ b/chrome/browser/ui/browser.cc
@@ -2131,6 +2131,7 @@ void Browser::RequestPpapiBrokerPermission(
     return;
   }
 
+#if !defined(OS_ANDROID)
   content_settings::TabSpecificContentSettings* tab_content_settings =
       content_settings::TabSpecificContentSettings::FromWebContents(
           web_contents);
@@ -2164,6 +2165,9 @@ void Browser::RequestPpapiBrokerPermission(
                          : base::UserMetricsAction("PPAPI.BrokerSettingDeny"));
   tab_content_settings->SetPepperBrokerAllowed(allowed);
   std::move(callback).Run(allowed);
+#else
+  std::move(callback).Run(false);
+#endif
 #endif
 }
 
@@ -2486,7 +2490,9 @@ void Browser::OnActiveTabChanged(WebContents* old_contents,
         session_id(), session_tab_helper->session_id(), base::TimeTicks::Now());
   }
 
+#if !defined(OS_ANDROID)
   SearchTabHelper::FromWebContents(new_contents)->OnTabActivated();
+#endif
 }
 
 void Browser::OnTabMoved(int from_index, int to_index) {
@@ -2789,6 +2795,7 @@ void Browser::SetAsDelegate(WebContents* web_contents, bool set_delegate) {
   web_contents->SetDelegate(delegate);
 
   // ...and all the helpers.
+#if !defined(OS_ANDROID)
   WebContentsModalDialogManager::FromWebContents(web_contents)
       ->SetDelegate(delegate);
   translate::ContentTranslateDriver* content_translate_driver =
@@ -2802,6 +2809,7 @@ void Browser::SetAsDelegate(WebContents* web_contents, bool set_delegate) {
     content_translate_driver->RemoveObserver(this);
     BookmarkTabHelper::FromWebContents(web_contents)->RemoveObserver(this);
   }
+#endif
 }
 
 void Browser::CloseFrame() {
