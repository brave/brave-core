diff --git a/chrome/browser/ui/unload_controller.h b/chrome/browser/ui/unload_controller.h
index 1c994941bdeabf521db6042b7e9233120bac6b3c..7e7a901c15a398e31a75d5ff67d070c5508010f2 100644
--- a/chrome/browser/ui/unload_controller.h
+++ b/chrome/browser/ui/unload_controller.h
@@ -100,6 +100,9 @@ class UnloadController : public WebContentsCollection::Observer,
 
   ~UnloadController() override;
 
+  bool confirmed_to_close() const { return confirmed_to_close_; }
+  void set_confirmed_to_close(bool close) { confirmed_to_close_ = close; }
+
   // Returns true if |contents| can be cleanly closed. When |browser_| is being
   // closed, this function will return false to indicate |contents| should not
   // be cleanly closed, since the fast shutdown path will just kill its
@@ -392,6 +395,10 @@ class UnloadController : public WebContentsCollection::Observer,
   // shortly (after a PostTask).
   bool is_delete_scheduled_ = false;
 
+  // Set to true when the user has allowed the browser window to
+  // close before running any warning or beforeunload handlers.
+  bool confirmed_to_close_ = false;
+
   base::WeakPtrFactory<UnloadController> weak_factory_{this};
 };
 
