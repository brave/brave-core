diff --git a/chrome/browser/download/download_core_service_impl.cc b/chrome/browser/download/download_core_service_impl.cc
index 083d3105be565327b7be2d167145eaec48d1e0b2..3e93064fefecaf5b520f570fd8ec0a30ffeedf00 100644
--- a/chrome/browser/download/download_core_service_impl.cc
+++ b/chrome/browser/download/download_core_service_impl.cc
@@ -56,9 +56,13 @@ DownloadCoreServiceImpl::GetDownloadManagerDelegate() {
 
   // In case the delegate has already been set by
   // SetDownloadManagerDelegateForTesting.
-  if (!manager_delegate_.get())
-    manager_delegate_ =
-        std::make_unique<ChromeDownloadManagerDelegate>(profile_);
+  if (!manager_delegate_.get()) {
+    #if defined(BRAVE_CREATE_DOWNLOAD_MANAGER_DELEGATE)
+        BRAVE_CREATE_DOWNLOAD_MANAGER_DELEGATE
+    #else
+        manager_delegate_ = std::make_unique<ChromeDownloadManagerDelegate>(profile_);
+    #endif
+  }
 
   manager_delegate_->SetDownloadManager(manager);
 
