diff --git a/third_party/blink/common/BUILD.gn b/third_party/blink/common/BUILD.gn
index 8e635b0da327aae8377bf2260113f395c835fa85..0d4e552a7b858ae06818e4d223277f65b6dec9de 100644
--- a/third_party/blink/common/BUILD.gn
+++ b/third_party/blink/common/BUILD.gn
@@ -2,6 +2,7 @@
 # Use of this source code is governed by a BSD-style license that can be
 # found in the LICENSE file.
 
+import("//brave/third_party/blink/common/sources.gni")
 import("//build/config/features.gni")
 import("//build/config/rust.gni")
 import("//build/nocompile.gni")
@@ -328,6 +329,7 @@ source_set("common") {
     "//ui/events/mojom:event_latency_metadata_mojom",
     "//ui/latency/mojom:shared_mojom_traits",
   ]
+  deps += brave_blink_common_deps
 
   # If use_blink is used then it needs to depend on //media
   if (use_blink) {
@@ -465,6 +467,7 @@ source_set("common_unittests_sources") {
     "//v8",
     "//v8:v8_libplatform",
   ]
+  deps += brave_blink_common_deps
 
   if (enable_nocompile_tests) {
     deps += [ ":blink_common_nocompile_tests" ]
