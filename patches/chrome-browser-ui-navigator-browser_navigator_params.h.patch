diff --git a/chrome/browser/ui/navigator/browser_navigator_params.h b/chrome/browser/ui/navigator/browser_navigator_params.h
index 541135be7009e4dc0f6557484e97ed1f24809ad9..bef9da69f165b99f44901302ed458ebab62bae87 100644
--- a/chrome/browser/ui/navigator/browser_navigator_params.h
+++ b/chrome/browser/ui/navigator/browser_navigator_params.h
@@ -72,6 +72,9 @@ struct OpenURLParams;
 
 // TODO(thestig): Split or ifdef out more fields that are not used on Android.
 struct NavigateParams {
+#if BUILDFLAG(ENABLE_CONTAINERS)
+  std::optional<content::StoragePartitionConfig> storage_partition_config;
+#endif
 #if BUILDFLAG(IS_ANDROID)
   explicit NavigateParams(
       std::unique_ptr<content::WebContents> contents_to_insert);
