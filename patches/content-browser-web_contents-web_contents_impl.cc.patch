diff --git a/content/browser/web_contents/web_contents_impl.cc b/content/browser/web_contents/web_contents_impl.cc
index 0fe5a498d6981962dba2f7de1603b3f0fd0c1ffa..5035130ea0cca2376fd7ecf34004af927a636c41 100644
--- a/content/browser/web_contents/web_contents_impl.cc
+++ b/content/browser/web_contents/web_contents_impl.cc
@@ -185,6 +185,10 @@
 #include "content/browser/media/session/pepper_playback_observer.h"
 #endif
 
+// #if BUILDFLAG(BRAVE_PAGE_GRAPH_ENABLED)
+#include "content/browser/devtools/devtools_instrumentation.h"
+// #endif
+
 namespace content {
 
 using AccessibilityEventCallback =
@@ -2220,6 +2224,15 @@ void WebContentsImpl::Activate() {
     delegate_->ActivateContents(this);
 }
 
+// #if BUILDFLAG(BRAVE_PAGE_GRAPH_ENABLED)
+void WebContentsImpl::FinalPageGraph(const std::string& frame_id,
+                                     const std::string& data) {
+  int const main_frame_tree_node_id = GetMainFrame()->GetFrameTreeNodeId();
+  devtools_instrumentation::FinalPageGraph(main_frame_tree_node_id,
+                                           frame_id, data);
+}
+// #endif
+
 void WebContentsImpl::LostCapture(RenderWidgetHostImpl* render_widget_host) {
   if (!RenderViewHostImpl::From(render_widget_host))
     return;
