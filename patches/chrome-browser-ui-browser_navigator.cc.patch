diff --git a/chrome/browser/ui/browser_navigator.cc b/chrome/browser/ui/browser_navigator.cc
index 923eb172b0fcfd9f396f30cce4dc91593960056e..dad7969e90bf6605382842342c8c66db332a3f9f 100644
--- a/chrome/browser/ui/browser_navigator.cc
+++ b/chrome/browser/ui/browser_navigator.cc
@@ -717,6 +717,9 @@ void Navigate(NavigateParams* params) {
 bool IsHostAllowedInIncognito(const GURL& url) {
   std::string scheme = url.scheme();
   base::StringPiece host = url.host_piece();
+#if defined(BRAVE_CHROMIUM_BUILD)
+  if (!IsHostAllowedInIncognitoBraveImpl(&scheme, host)) return false;
+#endif
   if (scheme == chrome::kChromeSearchScheme) {
     return host != chrome::kChromeUIThumbnailHost &&
            host != chrome::kChromeUIThumbnailHost2 &&
