diff --git a/chrome/browser/speech/on_device_speech_recognition_util.cc b/chrome/browser/speech/on_device_speech_recognition_util.cc
index 2b80c9563ed6ff469a40b42705ad9d43536b3cf8..68968ef20d3e577280b3b37dd53a90ac21290369 100644
--- a/chrome/browser/speech/on_device_speech_recognition_util.cc
+++ b/chrome/browser/speech/on_device_speech_recognition_util.cc
@@ -114,6 +114,7 @@ media::mojom::AvailabilityStatus GetOnDeviceSpeechRecognitionAvailabilityStatus(
     content::BrowserContext* context,
     std::string_view language,
     media::mojom::SpeechRecognitionQuality quality) {
+  return GetBraveOnDeviceSpeechRecognitionAvailabilityStatus(language, quality);
 #if BUILDFLAG(IS_ANDROID)
   return media::mojom::AvailabilityStatus::kUnavailable;
 #else
@@ -139,6 +140,9 @@ void GetOnDeviceSpeechRecognitionAvailabilityStatusAsync(
     std::string_view language,
     media::mojom::SpeechRecognitionQuality quality,
     base::OnceCallback<void(media::mojom::AvailabilityStatus)> callback) {
+  std::move(callback).Run(
+      GetBraveOnDeviceSpeechRecognitionAvailabilityStatus(language, quality));
+  return;
 #if BUILDFLAG(IS_ANDROID)
   std::move(callback).Run(media::mojom::AvailabilityStatus::kUnavailable);
 #else
