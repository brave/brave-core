diff --git a/content/browser/renderer_host/clipboard_host_impl.cc b/content/browser/renderer_host/clipboard_host_impl.cc
index 0def58240ad672d089f66772a18c7fa8ebfe709d..88487956e7c0a7f8923623dccb065edd8d102aa6 100644
--- a/content/browser/renderer_host/clipboard_host_impl.cc
+++ b/content/browser/renderer_host/clipboard_host_impl.cc
@@ -141,6 +141,9 @@ class FrameClipboardContext : public ClipboardHostImpl::Context {
   }
 #endif
 
+  void MaybeSanitizeClipboardText(bool& sanitize_on_next_write_text,
+                                  std::u16string& text) override;
+
  private:
   // The DocumentService that owns the host dies with the document.
   const raw_ref<RenderFrameHostImpl> render_frame_host_;
@@ -250,6 +253,10 @@ class ClipboardHostDocumentHelper
   }
 #endif
 
+  void SanitizeOnNextWriteText() override {
+    impl_->SanitizeOnNextWriteText();
+  }
+
  private:
   std::unique_ptr<ClipboardHostImpl> impl_;
 };
@@ -787,6 +794,8 @@ void ClipboardHostImpl::WriteText(const std::u16string& text) {
   ClipboardPasteData data;
   data.text = text;
   ++pending_writes_;
+  context_->MaybeSanitizeClipboardText(sanitize_on_next_write_text_,
+                                       data.text);
   GetContentClient()->browser()->IsClipboardCopyAllowedByPolicy(
       context_->CreateClipboardEndpoint(),
       {
