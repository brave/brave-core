diff --git a/chrome/browser/sync/chrome_sync_client.cc b/chrome/browser/sync/chrome_sync_client.cc
index b949c22d19dc7ac956ee283645c0a34f78b392be..2322e35f16c82e299eccb1bbc53e9eb063360342 100644
--- a/chrome/browser/sync/chrome_sync_client.cc
+++ b/chrome/browser/sync/chrome_sync_client.cc
@@ -6,7 +6,7 @@
 
 #include <string>
 #include <utility>
-
+  #include "base/debug/stack_trace.h"
 #include "base/bind.h"
 #include "base/feature_list.h"
 #include "base/path_service.h"
@@ -78,6 +78,8 @@
 #include "components/sync/base/sync_util.h"
 #include "components/sync/driver/model_type_controller.h"
 #include "components/sync/driver/sync_api_component_factory.h"
+#include "components/sync/driver/sync_service.h"
+#include "components/sync/driver/sync_user_settings.h"
 #include "components/sync/driver/syncable_service_based_model_type_controller.h"
 #include "components/sync/model/forwarding_model_type_controller_delegate.h"
 #include "components/sync/model/model_type_store.h"
@@ -148,6 +150,10 @@ using content::BrowserThread;
 using browser_sync::ExtensionModelTypeController;
 #endif  // BUILDFLAG(ENABLE_EXTENSIONS)
 
+namespace tricks {
+extern bool g_rewards_sync_initializing;
+}
+
 namespace browser_sync {
 
 namespace {
@@ -216,6 +222,7 @@ bool IsAppSyncEnabled(Profile* profile) {
 
 ChromeSyncClient::ChromeSyncClient(Profile* profile)
     : profile_(profile), extensions_activity_monitor_(profile) {
+LOG(ERROR) << "[BraveSync] " << " ChromeSyncClient::ChromeSyncClient " << " 000 this=" << this;
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
 
   profile_web_data_service_ =
@@ -249,7 +256,18 @@ ChromeSyncClient::ChromeSyncClient(Profile* profile)
       BookmarkSyncServiceFactory::GetForProfile(profile_));
 
 #if BUILDFLAG(IS_ANDROID)
-  trusted_vault_client_ = std::make_unique<TrustedVaultClientAndroid>();
+LOG(ERROR) << "[BraveSync] " << " ChromeSyncClient::ChromeSyncClient " << " 000-1 will call new TrustedVaultClientAndroid";
+LOG(ERROR) << "[BraveSync] " << " ChromeSyncClient::ChromeSyncClient " << " 001 this=" << this;
+LOG(ERROR) << "[BraveSync] " << " ChromeSyncClient::ChromeSyncClient " << " 002 tricks::g_rewards_sync_initializing=" << tricks::g_rewards_sync_initializing;
+
+LOG(ERROR) << "[BraveSync] " << __func__ << " stack=" << base::debug::StackTrace().ToString();
+
+  if (tricks::g_rewards_sync_initializing) {
+    // trusted_vault_client_ = nullptr
+  } else {
+    trusted_vault_client_ = std::make_unique<TrustedVaultClientAndroid>();
+  }    
+
 #else
   // TODO(crbug.com/1113597): consider destroying/notifying
   // |trusted_vault_client_| upon IdentityManager shutdown, to avoid its usages
@@ -672,6 +690,16 @@ void ChromeSyncClient::OnLocalSyncTransportDataCleared() {
   metrics::ClearDemographicsPrefs(profile_->GetPrefs());
 }
 
+void ChromeSyncClient::SetDefaultEnabledTypes(
+    syncer::SyncService* sync_service) {
+  DCHECK(sync_service);
+
+  // Default enabled types: Bookmarks
+  syncer::UserSelectableTypeSet selected_types;
+  selected_types.Put(syncer::UserSelectableType::kBookmarks);
+  sync_service->GetUserSettings()->SetSelectedTypes(false, selected_types);
+}
+
 #if BUILDFLAG(IS_CHROMEOS_LACROS)
 // static
 void ChromeSyncClient::SkipMainProfileCheckForTesting() {
