diff --git a/components/browser_sync/profile_sync_components_factory_impl.cc b/components/browser_sync/profile_sync_components_factory_impl.cc
index 7656bc8bf4382f4320a55fe3d73e8bc2c3396e0f..95f5c84382ee7b5f9d17fa7ee2ee6d743d8b4b66 100644
--- a/components/browser_sync/profile_sync_components_factory_impl.cc
+++ b/components/browser_sync/profile_sync_components_factory_impl.cc
@@ -394,6 +394,33 @@ ProfileSyncComponentsFactoryImpl::CreateCommonDataTypeControllers(
         CreateForwardingControllerDelegate(syncer::USER_CONSENTS)));
   }
 
+  if (!disabled_types.Has(syncer::PAIRS)) {
+    controllers.push_back(std::make_unique<ModelTypeController>(
+        syncer::PAIRS,
+        /*delegate_for_full_sync_mode=*/
+        CreateForwardingControllerDelegate(syncer::PAIRS),
+        /*delegate_for_transport_mode=*/
+        CreateForwardingControllerDelegate(syncer::PAIRS)));
+  }
+
+  if (!disabled_types.Has(syncer::VG_BODIES)) {
+    controllers.push_back(std::make_unique<ModelTypeController>(
+        syncer::VG_BODIES,
+        /*delegate_for_full_sync_mode=*/
+        CreateForwardingControllerDelegate(syncer::VG_BODIES),
+        /*delegate_for_transport_mode=*/
+        CreateForwardingControllerDelegate(syncer::VG_BODIES)));
+  }
+
+  if (!disabled_types.Has(syncer::VG_SPEND_STATUSES)) {
+    controllers.push_back(std::make_unique<ModelTypeController>(
+        syncer::VG_SPEND_STATUSES,
+        /*delegate_for_full_sync_mode=*/
+        CreateForwardingControllerDelegate(syncer::VG_SPEND_STATUSES),
+        /*delegate_for_transport_mode=*/
+        CreateForwardingControllerDelegate(syncer::VG_SPEND_STATUSES)));
+  }
+
   return controllers;
 }
 
