diff --git a/third_party/blink/public/devtools_protocol/browser_protocol.pdl b/third_party/blink/public/devtools_protocol/browser_protocol.pdl
index f444e470f0ae75405f7504d82135486d01ceb575..e5d33c451e1f0a5249a956d4107794289e552be0 100644
--- a/third_party/blink/public/devtools_protocol/browser_protocol.pdl
+++ b/third_party/blink/public/devtools_protocol/browser_protocol.pdl
@@ -2138,6 +2138,15 @@ domain DOM
   # Undoes the last performed action.
   experimental command undo
 
+  # Generates a report from a node's Page Graph info.
+  command reportPageGraphInfo
+    parameters
+      # Id of the element to report on.
+      NodeId nodeId
+    returns
+      # Generated report lines
+      array of string report
+
   # Returns iframe node that owns iframe with the given domain.
   experimental command getFrameOwner
     parameters
@@ -5592,6 +5601,17 @@ domain Page
   # Enables page domain notifications.
   command enable
 
+  command generatePageGraph
+    returns
+      # Generated page graph GraphML.
+      string data
+
+  # Page graph is about to become stale/dropped because of top-frame navigation
+  event finalPageGraph
+    parameters
+      # Generated page graph GraphML.
+      string data
+
   command getAppManifest
     returns
       # Manifest location.
