diff --git a/ios/web/web_state/web_state_impl_realized_web_state.mm b/ios/web/web_state/web_state_impl_realized_web_state.mm
index b3e98ba43b4ef18ef62f2c975e04aca6f1d70860..a52a60ce3dc86b131f3c920741716d607295c888 100644
--- a/ios/web/web_state/web_state_impl_realized_web_state.mm
+++ b/ios/web/web_state/web_state_impl_realized_web_state.mm
@@ -383,6 +383,7 @@ void WebStateImpl::RealizedWebState::OnFaviconUrlUpdated(
 }
 
 void WebStateImpl::RealizedWebState::CreateWebUI(const GURL& url) {
+  BRAVE_CREATE_WEBUI
   if (HasWebUI()) {
     if (web_ui_->GetController()->GetHost() == url.host()) {
       // Don't recreate webUI for the same host.
@@ -394,10 +395,12 @@ void WebStateImpl::RealizedWebState::CreateWebUI(const GURL& url) {
 }
 
 void WebStateImpl::RealizedWebState::ClearWebUI() {
+  BRAVE_CLEAR_WEBUI
   web_ui_.reset();
 }
 
 bool WebStateImpl::RealizedWebState::HasWebUI() const {
+  BRAVE_HAS_WEBUI
   return !!web_ui_;
 }
 
@@ -405,6 +408,7 @@ void WebStateImpl::RealizedWebState::HandleWebUIMessage(
     const GURL& source_url,
     std::string_view message,
     const base::Value::List& args) {
+  BRAVE_HANDLE_WEBUI_MESSAGE
   if (!HasWebUI()) {
     return;
   }
