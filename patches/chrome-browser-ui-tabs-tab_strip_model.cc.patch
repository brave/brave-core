diff --git a/chrome/browser/ui/tabs/tab_strip_model.cc b/chrome/browser/ui/tabs/tab_strip_model.cc
index 52faea724b7400a57ad66cfdc6f36c584cadaaed..42dfe0dac6fc89b6dce7f473c9b4b11ffcf315e9 100644
--- a/chrome/browser/ui/tabs/tab_strip_model.cc
+++ b/chrome/browser/ui/tabs/tab_strip_model.cc
@@ -1810,6 +1810,7 @@ void TabStripModel::AddTab(std::unique_ptr<tabs::TabModel> tab,
   if (ui::PageTransitionTypeIncludingQualifiersIs(transition,
                                                   ui::PAGE_TRANSITION_TYPED) &&
       index == count()) {
+    tab->set_opener_was_set_for_empty_new_tab();
     // Also, any tab opened at the end of the TabStrip with a "TYPED"
     // transition inherit opener as well. This covers the cases where the user
     // creates a New Tab (e.g. Ctrl+T, or clicks the New Tab button), or types
@@ -2577,6 +2578,15 @@ void TabStripModel::ExecuteContextMenuCommand(int context_index,
     return;
   }
   switch (command_id) {
+    case CommandRestoreTab:
+    case CommandBookmarkAllTabs:
+    case CommandShowVerticalTabs:
+    case CommandToggleTabMuted:
+    case CommandBringAllTabsToThisWindow:
+    case CommandCloseDuplicateTabs:
+    case CommandOpenInContainer:
+    case CommandRenameTab:
+      NOTREACHED();
     case CommandNewTabToRight: {
       base::UmaHistogramCounts1000(
           "Tab.ContextMenu.NewTabToRight.SelectedTabsCount",
@@ -4115,7 +4125,7 @@ void TabStripModel::MoveTabRelative(TabRelativeDirection direction) {
 
 std::pair<std::optional<int>, std::optional<int>>
 TabStripModel::GetAdjacentTabsAfterSelectedMove(
-    base::PassKey<DraggingTabsSession>,
+    base::PassKey<DraggingTabsSessionChromium>,
     int destination_index) {
   const int pinned_tab_count = IndexOfFirstNonPinnedTab();
   const std::vector<int> pinned_selected_indices = GetSelectedPinnedTabs();
@@ -4588,7 +4598,9 @@ void TabStripModel::InsertTabAtIndexImpl(
   }
 
   tabs::TabInterface* old_active_tab = GetActiveTab();
-  contents_data_->AddTabRecursive(std::move(tab_model), index, group, pin);
+  auto* opener = tab_model->opener_was_set_for_empty_new_tab() ? nullptr : tab_model->opener();
+  contents_data_->AddTabRecursive(std::move(tab_model), index, group, pin, opener);
+
   selection_model_.InvalidateListSelectionModel(base::PassKey<TabStripModel>());
 
   // Start computing selection change after updating the indices in
