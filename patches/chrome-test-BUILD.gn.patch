diff --git a/chrome/test/BUILD.gn b/chrome/test/BUILD.gn
index 167b043f9a1f7ec59d8e10705b4956b5647f6886..4efd4b6b3d31bd1f1abf9c4b6d64eaf361cbe1c6 100644
--- a/chrome/test/BUILD.gn
+++ b/chrome/test/BUILD.gn
@@ -2,6 +2,7 @@
 # Use of this source code is governed by a BSD-style license that can be
 # found in the LICENSE file.
 
+import("//brave/chromium_src/chrome/test/sources.gni")
 import("//build/cipd/cipd.gni")
 import("//build/config/buildflags_paint_preview.gni")
 import("//build/config/chrome_build.gni")
@@ -147,6 +148,7 @@ static_library("test_support") {
     "//chrome/browser/profiles:profile_manager",
     "//chrome/browser/ui/browser_window",
   ]
+  deps += brave_chromium_src_chrome_test_test_support_deps
 
   if (is_android || is_mac || is_win || is_chromeos) {
     deps += [
@@ -183,6 +185,7 @@ static_library("test_support") {
   }
 
   sources = [ "../app/startup_timestamps.h" ]
+  sources += brave_chromium_src_chrome_test_test_support_sources
 
   if (enable_guest_view) {
     public_deps += [ "//components/guest_view/browser:test_support" ]
@@ -1478,7 +1481,7 @@ if (is_android) {
       ]
     }
 
-    if (enable_desktop_android_extensions) {
+    if (is_desktop_android) {
       deps += [
         "//chrome/browser/extensions:browser_tests",
         "//chrome/browser/extensions/api/messaging:android_browsertests",
@@ -5529,6 +5532,7 @@ test("unit_tests") {
     "//ui/webui",
     "//v8",
   ]
+  deps += brave_chromium_src_chrome_test_unit_tests_deps
 
   if (enable_devtools_frontend) {
     deps += [ "//chrome/browser/ui/webui/devtools:unit_tests" ]
@@ -8091,6 +8095,7 @@ if (!is_android && !is_chromeos_device) {
     deps = [
       ":test_support",
       ":test_support_ui",
+      "//brave/test:browser_test_support",
       "//build/config/linux/dbus:buildflags",
       "//chrome:packed_resources",
       "//chrome:resources",
