diff --git a/chrome/browser/ui/views/tabs/tab.cc b/chrome/browser/ui/views/tabs/tab.cc
index ad55e68dc6e3f25c21a1e7c13c8bc093c5b0bd56..750a75bc53cebb46bf04f892ea16e14e31f1afe6 100644
--- a/chrome/browser/ui/views/tabs/tab.cc
+++ b/chrome/browser/ui/views/tabs/tab.cc
@@ -111,9 +111,7 @@ constexpr int kTabSeparatorTouchHeight = 24;
 //
 // The value returned here must be at least Tab::kMinimumEndcapWidth.
 float GetTabEndcapWidth() {
-  // TODO(pkasting): This should become a member function and vary with
-  // GetCornerRadius().
-  return GetLayoutInsets(TAB).left() - (MD::IsRefreshUi() ? 0.0f : 0.5f);
+  return MD::IsRefreshUi() ? GetLayoutInsets(TAB).left() : Tab::kMinimumEndcapWidth;
 }
 
 void DrawHighlight(gfx::Canvas* canvas,
