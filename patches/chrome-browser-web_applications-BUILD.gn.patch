diff --git a/chrome/browser/web_applications/BUILD.gn b/chrome/browser/web_applications/BUILD.gn
index 6181b1a742a2..3266469bab45 100644
--- a/chrome/browser/web_applications/BUILD.gn
+++ b/chrome/browser/web_applications/BUILD.gn
@@ -176,6 +176,7 @@ source_set("web_applications_test_support") {
     "//content/test:test_support",
     "//testing/gtest",
   ]
+  if (is_android) { deps -= ["//content/test:test_support",] }
 }
 
 source_set("web_applications_unit_tests") {
@@ -212,6 +213,7 @@ source_set("web_applications_unit_tests") {
     "//testing/gmock",
     "//testing/gtest",
   ]
+  if (is_android) { deps -= ["//chrome/test:test_support",] }
 
   if (is_chromeos) {
     deps += [
@@ -256,6 +258,7 @@ source_set("web_applications_browser_tests") {
     "//chrome/test:test_support_ui",
     "//components/permissions:permissions",
   ]
+  if (is_android) { deps -= ["//chrome/test:test_support", "//chrome/test:test_support_ui",] }
 
   if (is_chromeos) {
     deps += [
