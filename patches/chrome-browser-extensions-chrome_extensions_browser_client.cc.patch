diff --git a/chrome/browser/extensions/chrome_extensions_browser_client.cc b/chrome/browser/extensions/chrome_extensions_browser_client.cc
index 982247172ea41944b49e337d67f6850d2e806c16..9a55a0fd2a9505b3b70bbe127ad16603f88d5186 100644
--- a/chrome/browser/extensions/chrome_extensions_browser_client.cc
+++ b/chrome/browser/extensions/chrome_extensions_browser_client.cc
@@ -276,7 +276,7 @@ bool RegisterTransformers() {
 ChromeExtensionsBrowserClient::ChromeExtensionsBrowserClient()
     : resource_manager_(
           std::make_unique<ChromeComponentExtensionResourceManager>()),
-      api_client_(std::make_unique<ChromeExtensionsAPIClient>()),
+      api_client_(std::make_unique<BraveExtensionsAPIClient>()),
       event_router_forwarder_(base::MakeRefCounted<EventRouterForwarder>()) {
   AddAPIProvider(std::make_unique<CoreExtensionsBrowserAPIProvider>());
   AddAPIProvider(std::make_unique<ChromeExtensionsBrowserAPIProvider>());
@@ -676,6 +676,10 @@ void ChromeExtensionsBrowserClient::AttachExtensionTaskManagerTag(
 scoped_refptr<update_client::UpdateClient>
 ChromeExtensionsBrowserClient::CreateUpdateClient(
     scoped_refptr<update_client::Configurator> configurator) {
+  if (base::FeatureList::IsEnabled(
+    extensions_features::kBraveSequentialExtensionsUpdate)) {
+    return update_client::SequentialUpdateClientFactory(configurator);
+  }
   return update_client::UpdateClientFactory(configurator);
 }
 
@@ -691,7 +695,7 @@ ChromeExtensionsBrowserClient::CreateUpdateClientConfigurator(
       override_url = update_url;
     }
   }
-  return ChromeUpdateClientConfig::Create(context, override_url);
+  return BraveUpdateClientConfig::Create(context, override_url);
 }
 
 std::unique_ptr<ScopedBrowserContextKeepAlive>
