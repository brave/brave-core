diff --git a/content/renderer/render_frame_impl.h b/content/renderer/render_frame_impl.h
index 6a33dcf8df3d64f09a262fe62fff1bc7a310d0cb..aa786ce226f0893c87a0761a8c068e83314c5bf6 100644
--- a/content/renderer/render_frame_impl.h
+++ b/content/renderer/render_frame_impl.h
@@ -108,6 +108,8 @@
 #include "url/gurl.h"
 #include "url/origin.h"
 
+#include "brave/third_party/blink/brave_page_graph/buildflags/buildflags.h"
+
 #if defined(OS_MACOSX)
 #include "third_party/blink/public/mojom/clipboard/clipboard.mojom.h"
 #endif
@@ -469,6 +471,12 @@ class CONTENT_EXPORT RenderFrameImpl
                        const gfx::Range& range) override;
   void AddMessageToConsole(blink::mojom::ConsoleMessageLevel level,
                            const std::string& message) override;
+#if BUILDFLAG(BRAVE_PAGE_GRAPH_ENABLED)
+  void RegisterResourceBlockAd(const GURL& url,
+                               const std::string& rule);
+  void RegisterResourceBlockTracker(const GURL& url,
+                                    const std::string& host);
+#endif
   PreviewsState GetPreviewsState() override;
   bool IsPasting() override;
   bool IsBrowserSideNavigationPending() override;
@@ -730,6 +738,9 @@ class CONTENT_EXPORT RenderFrameImpl
   void OnMainFrameDocumentIntersectionChanged(
       const blink::WebRect& intersect_rect) override;
   void WillSendRequest(blink::WebURLRequest& request) override;
+#if BUILDFLAG(BRAVE_PAGE_GRAPH_ENABLED)
+  void RegisterRemoteFrameCreated(const GURL& url) override;
+#endif
   void DidLoadResourceFromMemoryCache(
       const blink::WebURLRequest& request,
       const blink::WebURLResponse& response) override;
@@ -1041,6 +1052,12 @@ class CONTENT_EXPORT RenderFrameImpl
   void OnMoveCaret(const gfx::Point& point);
   void OnScrollFocusedEditableNodeIntoRect(const gfx::Rect& rect);
   void OnSelectRange(const gfx::Point& base, const gfx::Point& extent);
+#if BUILDFLAG(BRAVE_PAGE_GRAPH_ENABLED)
+  void OnRegisterResourceBlockAd(const GURL& url,
+                                 const std::string& rule);
+  void OnRegisterResourceBlockTracker(const GURL& url,
+                                      const std::string& host);
+#endif
   void OnVisualStateRequest(uint64_t key);
   // TODO(https://crbug.com/995428): Deprecated.
   void OnReload();
