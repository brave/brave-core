diff --git a/chrome/browser/resources/settings/privacy_page/personalization_options.html b/chrome/browser/resources/settings/privacy_page/personalization_options.html
index f4968fb7c8f1463279e9284551b02d5c8eb03081..b88c9b63c35f466bdd5e69e14e5880972188e748 100644
--- a/chrome/browser/resources/settings/privacy_page/personalization_options.html
+++ b/chrome/browser/resources/settings/privacy_page/personalization_options.html
@@ -5,6 +5,7 @@ import {RestartType} from '../relaunch_mixin_lit.js';
 // </if>
 #html_wrapper_imports_end -->
 
+<if expr="_google_chrome">
 <if expr="not is_chromeos">
     <div id="chromeSigninUserChoiceSetting" class="hr cr-row" role="group"
         ?hidden="${!this.chromeSigninUserChoiceInfo_?.shouldShowSettings}"
@@ -47,6 +48,7 @@ import {RestartType} from '../relaunch_mixin_lit.js';
         no-set-pref>
     </settings-toggle-button>
 </if><!-- not chromeos -->
+</if>
 <if expr="_google_chrome">
 <if expr="is_chromeos">
     <!-- Ash Browser settings show a link to the OS settings instead. -->
@@ -58,6 +60,8 @@ import {RestartType} from '../relaunch_mixin_lit.js';
       </cr-link-row>
     ` : ''}
 </if><!-- chromeos -->
+</if><!-- _google_chrome -->
+<if expr="_google_chrome">
 <if expr="not is_chromeos">
     ${!this.shouldUseMetricsConsentRestructure_ ? html`
       <div class="cr-row hr">
@@ -88,12 +92,14 @@ import {RestartType} from '../relaunch_mixin_lit.js';
     ` : ''}
 </if><!-- not chromeos -->
 </if><!-- _google_chrome -->
+<if expr="_google_chrome">
     <settings-toggle-button id="urlCollectionToggle"
         class="hr"
         pref-key="url_keyed_anonymized_data_collection.enabled"
         label="$i18n{urlKeyedAnonymizedDataCollection}"
         sub-label="$i18n{urlKeyedAnonymizedDataCollectionDesc}">
     </settings-toggle-button>
+</if>
 <if expr="_google_chrome">
 <if expr="not is_chromeos">
     <settings-toggle-button id="spellCheckControl"
@@ -118,6 +124,7 @@ import {RestartType} from '../relaunch_mixin_lit.js';
     </cr-link-row>
 </if><!-- chromeos -->
 </if><!-- _google_chrome -->
+<if expr="_google_chrome">
     ${this.showSearchSuggestToggle_() ? html`
       <settings-toggle-button id="searchSuggestToggle"
           class="hr"
@@ -126,6 +133,7 @@ import {RestartType} from '../relaunch_mixin_lit.js';
           sub-label="$i18n{searchSuggestPrefDesc}">
       </settings-toggle-button>
     ` : ''}
+</if><!-- _google_chrome -->
 
     <!-- This toggle is only shown for users with the appropriate enterprise
         policy enabled. The state is not changeable by the user, so when shown,
@@ -149,6 +157,7 @@ import {RestartType} from '../relaunch_mixin_lit.js';
       </settings-toggle-button>
     ` : ''}
 
+<if expr="_google_chrome">
     ${this.showSignoutDialog_ ? html`
       <settings-signout-dialog .syncStatus="${this.syncStatus}"
           @close="${this.onSignoutDialogClose_}">
@@ -169,3 +178,4 @@ import {RestartType} from '../relaunch_mixin_lit.js';
       <div>$i18n{chromeSigninChoiceToast}</div>
     </cr-toast>
 </if>
+</if>
