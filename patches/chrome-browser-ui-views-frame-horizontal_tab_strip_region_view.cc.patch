diff --git a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
index f994a992e8eaa8124d8633239ec3f13cd43c4b62..def06d8fca1bfa6f91c7078cb70182235e46546f 100644
--- a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
+++ b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
@@ -86,6 +86,7 @@ class FrameGrabHandle : public views::View {
   void Layout(PassKey) override {
     LayoutSuperclass<views::View>(this);
 
+    if (!dialog_anchor_) { return; }
     int x = width() * 0.4;
     int y = height() * 0.7;
     dialog_anchor_->MaybeUpdateAnchor(gfx::Rect(x, y, 0, 0));
@@ -148,14 +149,14 @@ std::unique_ptr<TabStrip> CreateTabStrip(
             ->GetTabMenuModelFactory();
   }
 
-  auto tabstrip_controller = std::make_unique<BrowserTabStripController>(
+  auto tabstrip_controller = std::make_unique<BraveBrowserTabStripController>(
       browser_view->browser()->GetTabStripModel(), browser_view,
       std::move(tab_menu_model_factory));
 
-  std::unique_ptr<TabHoverCardController> hover_card_controller(
-      std::make_unique<TabHoverCardController>(tab_strip_region_view,
+  std::unique_ptr<BraveTabHoverCardController> hover_card_controller(
+      std::make_unique<BraveTabHoverCardController>(tab_strip_region_view,
                                                browser_view->browser()));
-  auto tab_strip = std::make_unique<TabStrip>(std::move(tabstrip_controller),
+  auto tab_strip = std::make_unique<BraveTabStrip>(std::move(tabstrip_controller),
                                               std::move(hover_card_controller));
   return tab_strip;
 }
@@ -232,7 +233,7 @@ HorizontalTabStripRegionViewOld::HorizontalTabStripRegionViewOld(
 
   if (ShouldShowNewTabButton(browser)) {
     std::unique_ptr<TabStripControlButton> tab_strip_control_button =
-        std::make_unique<NewTabButton>(
+        std::make_unique<BraveNewTabButton>(
             base::BindRepeating(&TabStrip::NewTabButtonPressed,
                                 base::Unretained(tab_strip_)),
             features::IsRoundedIconsEnabled() ? vector_icons::kAddWeight500Icon
@@ -476,14 +477,14 @@ void HorizontalTabStripRegionViewOld::InitializeTabStrip() {
   }
 
   tab_strip_->Initialize();
-  static_cast<BrowserTabStripController*>(tab_strip_->controller())
+  static_cast<BraveBrowserTabStripController*>(tab_strip_->controller())
       ->InitFromModel(tab_strip_);
   tab_strip_set_ = true;
 }
 
 void HorizontalTabStripRegionViewOld::ResetTabStrip() {
   tab_strip_set_ = false;
-  static_cast<BrowserTabStripController*>(tab_strip_->controller())->Reset();
+  static_cast<BraveBrowserTabStripController*>(tab_strip_->controller())->Reset();
   tab_strip_->Reset();
 }
 
@@ -606,7 +607,7 @@ void HorizontalTabStripRegionViewOld::UpdateButtonBorders() {
   const int extra_vertical_space =
       GetLayoutConstant(LayoutConstant::kTabStripHeight) -
       GetLayoutConstant(LayoutConstant::kTabstripToolbarOverlap) -
-      NewTabButton::kButtonSize.height();
+      BraveNewTabButton::GetButtonSize().height();
   const int top_inset = extra_vertical_space / 2;
   const int bottom_inset =
       extra_vertical_space - top_inset +
@@ -899,13 +900,16 @@ void HorizontalTabStripRegionViewNew::OnTabStripViewSet() {
   ReorderChildView(tab_strip_view(), index);
 }
 
-BEGIN_METADATA(HorizontalTabStripRegionViewNew)
-END_METADATA
-
 std::unique_ptr<TabStripRegionView> CreateHorizontalTabStripRegionView(
     BrowserView* browser_view) {
+  if (!base::FeatureList::IsEnabled(tabs::kTabStripUnification)) {
+    return std::make_unique<BraveHorizontalTabStripRegionView>(browser_view);
+  }
   if (base::FeatureList::IsEnabled(tabs::kTabStripUnification)) {
     return std::make_unique<HorizontalTabStripRegionViewNew>(browser_view);
   }
   return std::make_unique<HorizontalTabStripRegionViewOld>(browser_view);
 }
+
+BEGIN_METADATA(HorizontalTabStripRegionViewNew)
+END_METADATA
