diff --git a/chrome/browser/ui/color/omnibox_color_mixer.cc b/chrome/browser/ui/color/omnibox_color_mixer.cc
index 3a4e4aae8bb14e074b14e1f7f100cf8f476825fd..948d85681e7e3a79384806abc1031d041836cd5e 100644
--- a/chrome/browser/ui/color/omnibox_color_mixer.cc
+++ b/chrome/browser/ui/color/omnibox_color_mixer.cc
@@ -254,6 +254,7 @@ void ApplyOmniboxCR2023Colors(ui::ColorMixer& mixer,
 
 void AddOmniboxColorMixer(ui::ColorProvider* provider,
                           const ui::ColorProviderKey& key) {
+  [&]() -> void {
   ui::ColorMixer& mixer = provider->AddMixer();
 
   const bool high_contrast_custom_handling = ShouldApplyHighContrastColors(key);
@@ -518,4 +519,8 @@ void AddOmniboxColorMixer(ui::ColorProvider* provider,
 
   // Apply specific baseline colors for the composebox submit button.
   ApplyComposeboxBaselineColors(mixer, key);
+  }();
+  #if !BUILDFLAG(IS_ANDROID)
+  AddBraveOmniboxColorMixer(provider, key);
+  #endif  // #if !BUILDFLAG(IS_ANDROID)
 }
