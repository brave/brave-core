diff --git a/build/util/android_chrome_version.py b/build/util/android_chrome_version.py
index f00cd99d2bfa5793d6ed5d96025885ad83af412f..41f44538d04232f990d0af59682b92f43fe1fa23 100755
--- a/build/util/android_chrome_version.py
+++ b/build/util/android_chrome_version.py
@@ -48,6 +48,19 @@ For example:
 import argparse
 from collections import namedtuple
 
+import os.path
+import sys
+
+_BUILD_ANDROID = os.path.join(os.path.dirname(__file__), os.pardir, 'android')
+sys.path.append(_BUILD_ANDROID)
+
+from pylib import constants
+from pylib.constants import host_paths
+_BRAVE_SCRIPT_PATH = os.path.join(host_paths.DIR_SOURCE_ROOT, 'brave',
+                                'script')
+sys.path.append(_BRAVE_SCRIPT_PATH)
+import brave_version_utils
+
 # Package name version bits.
 _PACKAGE_NAMES = {
     'CHROME': 0,
@@ -199,6 +212,7 @@ def _GetAbisToDigitMask(build_number):
     mapped to version code suffix.
   """
 
+  from brave_version_utils import remap_build_number; build_number = remap_build_number(build_number)
   if build_number < 5750:
     return {
         'arm': {
@@ -327,7 +341,7 @@ def GenerateVersionCodes(version_values, arch, is_next_build):
   """
 
   base_version_code = int(
-      '%s%03d00' % (version_values['BUILD'], int(version_values['PATCH'])))
+      '%02d%02d%03d00' % (int(version_values['MINOR']) + 41, int(version_values['BUILD']), int(version_values['PATCH'])))
 
   if is_next_build:
     base_version_code += _NEXT_BUILD_VERSION_CODE_DIFF
