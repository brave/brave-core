diff --git a/base/trace_event/memory_infra_background_allowlist.cc b/base/trace_event/memory_infra_background_allowlist.cc
index cf7123aaa5590e291db57c7aae20f562089c3626..7dc3bcbe0793e0ea8d0449bc96ec32f7ebd16da1 100644
--- a/base/trace_event/memory_infra_background_allowlist.cc
+++ b/base/trace_event/memory_infra_background_allowlist.cc
@@ -385,6 +385,9 @@ bool IsMemoryDumpProviderInAllowlist(const char* mdp_name) {
 }
 
 bool IsMemoryAllocatorDumpNameInAllowlist(const std::string& name) {
+  if (IsMemoryAllocatorDumpNameInBraveAllowlist(name)) {
+    return true;
+  }
   // Global dumps that are of hex digits are all allowed for background use.
   if (base::StartsWith(name, "global/", CompareCase::SENSITIVE)) {
     for (size_t i = strlen("global/"); i < name.size(); i++) {
