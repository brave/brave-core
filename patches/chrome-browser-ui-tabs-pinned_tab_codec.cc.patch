diff --git a/chrome/browser/ui/tabs/pinned_tab_codec.cc b/chrome/browser/ui/tabs/pinned_tab_codec.cc
index cc444160cbba45a26dd68a86e2617b8ebd9da0a1..79e256d092fe3b8c66e4808396b8a4ab65427b61 100644
--- a/chrome/browser/ui/tabs/pinned_tab_codec.cc
+++ b/chrome/browser/ui/tabs/pinned_tab_codec.cc
@@ -49,6 +49,7 @@ void EncodePinnedTabs(BrowserWindowInterface* browser,
         web_contents->GetController().GetLastCommittedEntry();
     if (entry) {
       serialized_tabs.Append(EncodeTab(entry->GetURL()));
+      BraveModifyEncodedPinnedTab(web_contents, serialized_tabs.back());
     }
   }
 }
@@ -120,6 +121,7 @@ StartupTabs PinnedTabCodec::ReadPinnedTabs(Profile* profile) {
     }
     std::optional<StartupTab> tab = DecodeTab(serialized_tab.GetDict());
     if (tab.has_value()) {
+      BraveModifyDecodedPinnedTab(serialized_tab, tab.value());
       results.push_back(tab.value());
     }
   }
