diff --git a/chrome/android/java/src/org/chromium/chrome/browser/fullscreen/FullscreenHtmlApiHandlerBase.java b/chrome/android/java/src/org/chromium/chrome/browser/fullscreen/FullscreenHtmlApiHandlerBase.java
index b2b82e978c93c9a2071b562298fd61ee3239e243..65367bc23312aaab47de850f984f3a8a2ff43bcf 100644
--- a/chrome/android/java/src/org/chromium/chrome/browser/fullscreen/FullscreenHtmlApiHandlerBase.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/fullscreen/FullscreenHtmlApiHandlerBase.java
@@ -325,6 +325,11 @@ public abstract class FullscreenHtmlApiHandlerBase
 
                     @Override
                     public void onHidden(Tab tab, @TabHidingType int reason) {
+                        if (BraveFullscreenHtmlApiHandlerBase.class.cast(
+                                        FullscreenHtmlApiHandlerBase.this)
+                                .maybeSkipExitFullscreenOnTabHidden(mActivity, reason)) {
+                            return;
+                        }
                         // Clean up any fullscreen state that might impact other tabs.
                         mFullscreenManagerDelegate.onExitFullscreen(null);
                     }
@@ -902,6 +907,10 @@ public abstract class FullscreenHtmlApiHandlerBase
     @Override
     public void onActivityStateChange(Activity activity, int newState) {
         if (newState == ActivityState.STOPPED && mExitFullscreenOnStop) {
+            if (BraveFullscreenHtmlApiHandlerBase.class.cast(FullscreenHtmlApiHandlerBase.this)
+                        .shouldPreservePersistentFullscreenForPictureInPicture(activity)) {
+                return;
+            }
             // Exit fullscreen in onStop to ensure the system UI flags are set
             // correctly when
             // showing again (on JB MR2+ builds, the omnibox would be covered by the
