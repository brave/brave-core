diff --git a/components/sync/driver/profile_sync_service.cc b/components/sync/driver/profile_sync_service.cc
index 96abaa6e76e73d8896c31063a532b07a164ac9d0..de65469abaf8522b57733e6b5f7382346d969c3d 100644
--- a/components/sync/driver/profile_sync_service.cc
+++ b/components/sync/driver/profile_sync_service.cc
@@ -199,6 +199,7 @@ ProfileSyncService::ProfileSyncService(InitParams init_params)
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   DCHECK(sync_client_);
   DCHECK(IsLocalSyncEnabled() || identity_manager_ != nullptr);
+  BRAVE_PROFILE_SYNC_SERVICE
 
   // If Sync is disabled via command line flag, then ProfileSyncService
   // shouldn't be instantiated.
@@ -233,6 +234,7 @@ ProfileSyncService::ProfileSyncService(InitParams init_params)
 
 ProfileSyncService::~ProfileSyncService() {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
+  BRAVE_D_PROFILE_SYNC_SERVICE
   if (identity_manager_)
     identity_manager_->RemoveObserver(this);
   sync_prefs_.RemoveSyncPrefObserver(this);
@@ -547,6 +549,7 @@ void ProfileSyncService::StartUpSlowEngineComponents() {
   params.report_unrecoverable_error_function =
       base::BindRepeating(ReportUnrecoverableError, channel_);
   sync_prefs_.GetInvalidationVersions(&params.invalidation_versions);
+  BRAVE_SET_POLL_INTERVAL
   params.poll_interval = sync_prefs_.GetPollInterval();
   if (params.poll_interval.is_zero()) {
     params.poll_interval =
@@ -554,6 +557,7 @@ void ProfileSyncService::StartUpSlowEngineComponents() {
   }
 
   if (!IsLocalSyncEnabled()) {
+    BRAVE_START_UP_SLOW_ENGINE_COMPONENTS
     auth_manager_->ConnectionOpened();
   }
 
@@ -674,6 +678,7 @@ void ProfileSyncService::StopImpl(SyncStopDataFate data_fate) {
       // Note: We do this after shutting down, so that notifications about the
       // changed pref values don't mess up our state.
       sync_prefs_.ClearPreferences();
+      BRAVE_STOP_IMPL
       break;
   }
 }
@@ -928,6 +933,7 @@ void ProfileSyncService::OnEngineInitialized(
           engine_.get(), this);
 
   crypto_.SetSyncEngine(GetAuthenticatedAccountInfo(), engine_.get());
+  BRAVE_ON_ENGINE_INITIALIZED
 
   // Auto-start means IsFirstSetupComplete gets set automatically.
   if (start_behavior_ == AUTO_START &&
@@ -953,7 +959,7 @@ void ProfileSyncService::OnEngineInitialized(
                                 GoogleServiceAuthError::AuthErrorNone());
     }
   }
-
+  
   NotifyObservers();
 }
 
@@ -1228,6 +1234,7 @@ void ProfileSyncService::TriggerRefresh(const ModelTypeSet& types) {
 }
 
 bool ProfileSyncService::IsSignedIn() const {
+  BRAVE_IS_SIGNED_IN
   // Sync is logged in if there is a non-empty account id.
   return !GetAuthenticatedAccountInfo().account_id.empty();
 }
