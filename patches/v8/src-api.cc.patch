diff --git a/src/api.cc b/src/api.cc
index a52fadc2eef6bcecf73c81463c3665f6313fea72..a06d1bec5fcc54b865dc8083e3e65c8c7c52320b 100644
--- a/src/api.cc
+++ b/src/api.cc
@@ -125,6 +125,8 @@
 #endif  // V8_TARGET_ARCH_X64
 #endif  // V8_OS_WIN
 
+#include "brave/third_party/blink/brave_page_graph/buildflags/buildflags.h"
+
 namespace v8 {
 
 /*
@@ -7973,6 +7975,18 @@ void Isolate::CheckMemoryPressure() {
   heap->CheckMemoryPressure();
 }
 
+#if BUILDFLAG(BRAVE_PAGE_GRAPH_ENABLED)
+void Isolate::RegisterScriptStartFunc(const ScriptStartFunc func) {
+  i::Isolate* internal_isolate = reinterpret_cast<i::Isolate*>(this);
+  internal_isolate->SetRegisterScriptStartFunc(func);
+}
+
+void Isolate::RegisterScriptEndFunc(const ScriptStartFunc func) {
+  i::Isolate* internal_isolate = reinterpret_cast<i::Isolate*>(this);
+  internal_isolate->SetRegisterScriptEndFunc(func);
+}
+#endif
+
 HeapProfiler* Isolate::GetHeapProfiler() {
   i::HeapProfiler* heap_profiler =
       reinterpret_cast<i::Isolate*>(this)->heap_profiler();
