diff --git a/test/cctest/heap/test-heap.cc b/test/cctest/heap/test-heap.cc
index b9d6a24a46738e7ee0fdffa8395230ef6265e438..44e6c4eb98d59978cf3ae1eec0143dae23203f96 100644
--- a/test/cctest/heap/test-heap.cc
+++ b/test/cctest/heap/test-heap.cc
@@ -1489,7 +1489,7 @@ TEST(CompilationCacheCachingBehavior) {
     MaybeHandle<SharedFunctionInfo> cached_script =
         compilation_cache->LookupScript(source, Handle<Object>(), 0, 0,
                                         v8::ScriptOriginOptions(true, false),
-                                        language_mode);
+                                        language_mode, 0);
     CHECK(!cached_script.is_null());
   }
 
@@ -1500,7 +1500,7 @@ TEST(CompilationCacheCachingBehavior) {
     MaybeHandle<SharedFunctionInfo> cached_script =
         compilation_cache->LookupScript(source, Handle<Object>(), 0, 0,
                                         v8::ScriptOriginOptions(true, false),
-                                        language_mode);
+                                        language_mode, 0);
     CHECK(!cached_script.is_null());
 
     // Progress code age until it's old and ready for GC.
@@ -1520,7 +1520,7 @@ TEST(CompilationCacheCachingBehavior) {
     MaybeHandle<SharedFunctionInfo> cached_script =
         compilation_cache->LookupScript(source, Handle<Object>(), 0, 0,
                                         v8::ScriptOriginOptions(true, false),
-                                        language_mode);
+                                        language_mode, 0);
     CHECK(cached_script.is_null());
   }
 }
