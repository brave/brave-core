diff --git a/components/sync_device_info/device_info_sync_bridge.cc b/components/sync_device_info/device_info_sync_bridge.cc
index 0463687c9ec004a74d685ebd6281ea9bd18d3594..338baba9198bc0ad5881524d20f5376b46d83c77 100644
--- a/components/sync_device_info/device_info_sync_bridge.cc
+++ b/components/sync_device_info/device_info_sync_bridge.cc
@@ -234,7 +234,8 @@ DeviceInfo SpecificsToModel(const DeviceInfoSpecifics& specifics) {
       SpecificsToDesktopToIOSPromoReceivingTypes(specifics),
       SpecificsToGlicExperimentalTriggeringState(specifics),
       SpecificsToGlicExperimentalTriggeringVersion(specifics),
-      android_os_build_fingerprint_prefix);
+      android_os_build_fingerprint_prefix,
+      SpecificsToSelfDeleteSupport(specifics));
 }
 
 // Allocate a EntityData and copies |specifics| into it.
@@ -260,7 +261,7 @@ sync_pb::PhoneAsASecurityKeySpecificFields PhoneAsASecurityKeyInfoToProto(
 }
 
 // Converts a local DeviceInfo into a freshly allocated DeviceInfoSpecifics.
-std::unique_ptr<DeviceInfoSpecifics> MakeLocalDeviceSpecifics(
+std::unique_ptr<DeviceInfoSpecifics> MakeLocalDeviceSpecifics_ChromiumImpl(
     const DeviceInfo& info) {
   auto specifics = std::make_unique<DeviceInfoSpecifics>();
   specifics->set_cache_guid(info.guid());
@@ -489,7 +490,7 @@ LocalDeviceInfoProvider* DeviceInfoSyncBridge::GetLocalDeviceInfoProvider() {
   return local_device_info_provider_.get();
 }
 
-void DeviceInfoSyncBridge::RefreshLocalDeviceInfoIfNeeded() {
+void DeviceInfoSyncBridge::RefreshLocalDeviceInfoIfNeeded_ChromiumImpl() {
   // Device info cannot be synced if the provider is not initialized. When it
   // gets initialized, local device info will be sent.
   if (!local_device_info_provider_->GetLocalDeviceInfo()) {
@@ -585,17 +586,19 @@ std::optional<ModelError> DeviceInfoSyncBridge::ApplyIncrementalSyncChanges(
   for (const std::unique_ptr<EntityChange>& change : entity_changes) {
     const std::string guid = change->storage_key();
 
+#if 0  // Brave: re-upload defeats removing other devices in the sync chain
     // Reupload local device if it was deleted from the server.
     if (local_cache_guid_ == guid &&
         change->type() == EntityChange::ACTION_DELETE) {
       has_tombstone_for_local_device = true;
       continue;
     }
+#endif
 
     // Ignore any remote changes that have a cache guid that is or was this
     // local device.
     if (device_info_prefs_->IsRecentLocalCacheGuid(guid)) {
-      continue;
+      if (change->type() != EntityChange::ACTION_DELETE) continue;
     }
 
     if (change->type() == EntityChange::ACTION_DELETE) {
@@ -947,6 +950,10 @@ void DeviceInfoSyncBridge::OnReadAllMetadata(
     return;
   }
 
+  if (!device_info_prefs_->IsResetDevicesProgressTokenDone()) {
+    metadata_batch->ClearProgressToken();
+    device_info_prefs_->SetResetDevicesProgressTokenDone();
+  }
   // In the regular case for sync being disabled, wait for MergeFullSyncData()
   // before initializing the LocalDeviceInfoProvider.
   if (!syncer::IsInitialSyncDone(
@@ -1196,6 +1203,7 @@ DeviceInfoSyncBridge::CountActiveDevicesByType() const {
 }
 
 void DeviceInfoSyncBridge::ExpireOldEntries() {
+  return;
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   CHECK(store_);
   TRACE_EVENT0("sync", "DeviceInfoSyncBridge::ExpireOldEntries");
