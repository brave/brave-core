# pnpm configuration - only settings that MUST be in .npmrc
# Other pnpm settings are in package.json under the "pnpm" key

# Use hoisted node_modules structure for compatibility with webpack and
# packages that rely on hoisting. This setting cannot be in package.json.
node-linker=hoisted
