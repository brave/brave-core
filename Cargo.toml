[package]
name = "adblock-ffi"
version = "0.1.0"
authors = ["Brian R. Bondy <netzen@gmail.com>"]
edition = "2018"

[dependencies]
adblock="0.1.0"

[lib]
crate-type = [
    "cdylib",
    "staticlib",
]
name = "adblock"

[patch.crates-io]
adblock = { path = "../adblock-rust" }

[features]
cbindgen = []
